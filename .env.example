# Set these in Railway/Render env vars — NEVER commit real values
DISCORD_BOT_TOKEN=
DISCORD_CLIENT_ID=
MONGODB_URI=
GEMINI_API_KEY=
# Optional Bedrock server for /server command
BEDROCK_HOST=play.example.net
BEDROCK_PORT=19132
# Channel IDs (optional defaults — can be set per-guild via dashboard)
DEFAULT_LOG_CHANNEL=
